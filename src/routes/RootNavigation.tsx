import * as React from 'react';

// Create a reference (navigationRef) using React.createRef()
// This reference will be used to programmatically access navigation actions.
export const navigationRef: any = React.createRef();

// The above code creates a reference object that allows you to interact with React Navigation
